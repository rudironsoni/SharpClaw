apiVersion: v1
kind: Service
metadata:
  name: sharpclaw-host
  namespace: default
spec:
  selector:
    app: sharpclaw-host
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
